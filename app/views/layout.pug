doctype
html
  head
    meta(charset="utf-8")
    title #{title}
    block css
    include includes/head
  body
    if user
      input(id="INITIAL_STATE_LOG",type="hidden",value=user.name)
    block content
